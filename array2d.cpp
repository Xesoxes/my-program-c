#include <iostream>
#include <windows.h>
using namespace std;

//Apakah ini menggunakan function array?
//maybe.

void gotoxy(int x, int y){ //function untuk membuat jarak karakter.
	COORD c;
	c.X=x;
	c.Y=y;
	SetConsoleCursorPosition(GetStdHandle(STD_OUTPUT_HANDLE),c);
}

class Textstart {
	public:
		string kata1;
		string kata2;
		string kata3;
		Textstart(string x, string y, string g){
			kata1 = x;
			kata2 = y;
			kata3 = y;


			#include <iostream>
#include <windows.h>
using namespace std;

//Apakah ini menggunakan function array?
//maybe.

void gotoxy(int x, int y){ //function untuk membuat jarak karakter.
	COORD c;
	c.X=x;
	c.Y=y;
	SetConsoleCursorPosition(GetStdHandle(STD_OUTPUT_HANDLE),c);
}

class Textstart {
	public:
		string kata1;
		string kata2;
		string kata3;
		Textstart(string x, string y, string g){
			kata1 = x;
			kata2 = y;
			kata3 = g;
			
		
		}

};


int main(){
	Textstart text("HAI !!!", "Apa Kabar?", "Baik baik saja kan disana ?");
	
	gotoxy(10,3);
	cout << "PESAN";
	

	int x=0;
	gotoxy(10,5);
	while (text.kata1[x] != '\0' )
	{
		cout << text.kata1[x];
		Sleep(100);
		x++;
	}	
	

	int y=0;
	gotoxy(10,6);
	while (text.kata2[y] != '\0' )
	{
		cout << text.kata2[y];
		Sleep(100);
		y++;
	}
	
	int g=0;
	gotoxy(10,7);
	while (text.kata3[g] != '\0' )
	{
		cout << text.kata3[g];
		Sleep(100);
		g++;
	}
		
	
	
	
	
system("pause>0");
return 0;
}

		
		}

};


int main(){
	Textstart text("HAI !!!", "Apa Kabar?", "Baik baik saja kan disana ?");
	
	gotoxy(10,3);
	cout << "PESAN";
	

	int x=0;
	gotoxy(10,5);
	while (text.kata1[x] != '\0' )
	{
		cout << text.kata1[x];
		Sleep(100);
		x++;
	}	
	

	int y=0;
	gotoxy(10,6);
	while (text.kata2[y] != '\0' )
	{
		cout << text.kata2[y];
		Sleep(100);
		y++;
	}
	
	int g=0;
	gotoxy(10,7);
	while (text.kata3[g] != '\0' )
	{
		cout << text.kata3[g];
		Sleep(100);
		g++;
		#include <iostream>
#include <windows.h>
using namespace std;

//Apakah ini menggunakan function array?
//maybe.

void gotoxy(int x, int y){ //function untuk membuat jarak karakter.
	COORD c;
	c.X=x;
	c.Y=y;
	SetConsoleCursorPosition(GetStdHandle(STD_OUTPUT_HANDLE),c);
}

class Textstart {
	public:
		string kata1;
		string kata2;
		string kata3;
		Textstart(string x, string y, string g){
			kata1 = x;
			kata2 = y;
			kata3 = g;
		
		}

};


int main(){
	Textstart text("HAI !!!", "Apa Kabar?", "Baik baik saja kan disana ?");
	
	gotoxy(10,3);
	cout << "PESAN";
	

	int x=0;
	gotoxy(10,5);
	while (text.kata1[x] != '\0' )
	{
		cout << text.kata1[x];
		Sleep(100);
		x++;
	}	
	

	int y=0;
	gotoxy(10,6);
	while (text.kata2[y] != '\0' )
	{
		cout << text.kata2[y];
		Sleep(100);
		y++;
	}
	
	int g=0;
	gotoxy(10,7);
	while (text.kata3[g] != '\0' )
	{
		cout << text.kata3[g];
		Sleep(100);
		g++;
	}
		
	
	
	
	
system("pause>0");
return 0;
}

	}
		
	
	
	
	
system("pause>0");
return 0;
}
